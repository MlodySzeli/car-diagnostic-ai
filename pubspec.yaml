name: car_diagnostic_ai
description: Car Diagnostic AI - diagnostyka samochodowa
publish_to: "none"

version: 4.0.0+4

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  sqflite_common_ffi: ^2.3.4
  path: ^1.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/data/vehicles.json
    - assets/data/diagnostic_data.json
